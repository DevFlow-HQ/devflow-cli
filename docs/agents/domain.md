# Domain Docs

This is a single-context repository. Before exploring or changing a domain area, read:

- `CONTEXT.md` for the canonical domain vocabulary.
- Relevant decisions under `docs/adr/`.

If either is absent, proceed silently. Create or amend domain documentation only when terminology or a durable decision is actually resolved.

## Vocabulary

Use the glossary's terms in issue titles, designs, tests, and code. If a needed concept is missing, reconsider the terminology or identify it as a domain-modeling gap.

## Decision Conflicts

Surface any proposal that contradicts an existing ADR rather than silently overriding it.
